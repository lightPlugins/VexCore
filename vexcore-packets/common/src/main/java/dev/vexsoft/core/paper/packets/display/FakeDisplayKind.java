package dev.vexsoft.core.paper.packets.display;

/** Identifies the client-side entity kind represented by a fake display handle. */
public enum FakeDisplayKind {
  BLOCK,
  ITEM,
  TEXT,
  INTERACTION
}
