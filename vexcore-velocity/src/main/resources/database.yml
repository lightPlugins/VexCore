# Velocity always participates in a multi-process setup and therefore requires PostgreSQL.
storage: postgresql

postgresql:
  jdbc-url: jdbc:postgresql://localhost:5432/vexcore
  username: postgres
  password: change-me
  maximum-pool-size: 5
  auto-create-database: true
  maintenance-database: postgres
